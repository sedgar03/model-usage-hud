#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${ROOT_DIR}${PYTHONPATH:+:${PYTHONPATH}}"

# Default to your preferred mode; pass extra flags to override behavior.
exec python3 "${ROOT_DIR}/usage_hud.py" --mini --force "$@"
